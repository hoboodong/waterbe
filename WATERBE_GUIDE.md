# 워터비 운영 시스템 가이드

## 역할

```text
앱·매출·운영 입력 → Supabase 수집·저장 → 워터비 연결·분석
```

워터비는 매장 운영의 두뇌다. 데이터를 직접 원본으로 만들지 않고 Supabase에 수집된 데이터를 연결해 일·주·월·년 단위의 운영 분석과 개선점을 제공한다.

## 연결 기준

- 상품: `storeId + appProductId`
- 원재료: `storeId + appIngredientId`
- 레시피: `appRecipeId`

매출 원본의 `salesCodeName`은 판매 집계 코드명이며 여러 상품이 공유할 수 있다. 공유 코드명은 상품별로 나누지 않고 코드명 단위로 분석한다.

`SalesCodeMapping`으로 매출 코드명을 앱의 `martCode`에 연결한다.

## 수집 데이터

| 영역 | 입력·수집 위치 |
| --- | --- |
| 상품·원재료·레시피 | 앱 → Supabase |
| 생산계획·생산기록 | 앱 → Supabase |
| 할인라벨 출력수량·시간·가격 | 앱 → Supabase 자동 기록 |
| 코드명별 판매수량·매출 | 워터비 일일 매출 데이터 |
| 남선푸드 발주·기타발주·입고 | Supabase |
| 비품·카드 지출 | Supabase |
| 원재료 재고실사 | Supabase |
| 직원·일정 | Supabase |

## 일일 상품 분석

```text
생산량 + 할인라벨 출력수량·할인률 + 코드별 판매수량·매출 + 폐기수량
```

- 판매율 = 판매수량 ÷ 실제생산량
- 평균판매단가 = 매출 ÷ 판매수량
- 미판매 추정 = 실제생산량 - 판매수량 - 폐기수량
- 할인라벨 출력은 판매 확정값이 아니라 운영 참고값이다.

## 운영 원칙

- 완제품은 당일 생산·당일 판매가 원칙이다.
- 원재료 재고는 매일 실사하지 않고, 입고·레시피 기준 생산 사용량으로 계산한 뒤 주기적으로 실사한다.
- 레시피·발주단가·지출·입고·재고·생산 기록은 날짜 이력을 남긴다.
- 과거 기록은 오류 정정 외에는 덮어쓰지 않는다.

## 분석 범위

| 단위 | 내용 |
| --- | --- |
| 일 | 생산·판매·할인·폐기 이상 확인 |
| 주 | 상품별 판매 추이와 발주 필요량 |
| 월 | 매출·원가·지출·인건비·마진 |
| 년 | 매장·상품·업체별 성장과 계절성 |

매출 원본과 조회 규칙은 `instances/sales/README.md`를 따른다.
