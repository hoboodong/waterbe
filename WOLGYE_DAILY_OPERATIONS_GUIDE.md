# 월계점 일일 생산·마감할인 운영 가이드

월계점의 생산 투입량, 완성품, 시코드 할인 라벨 인쇄기록, 다음 날 미사용 라벨과 폐기를 한 영업일 단위로 연결한다.

## 데이터 원칙

- 시코드 Supabase 인쇄기록은 할인 라벨을 **출력한 기록**이다. 판매기록이 아니다.
- 오출력도 원본에서 삭제하지 않는다. 현장 확인을 통해 별도로 표시한다.
- 저장소의 일일 운영기록에는 Supabase 원본을 복제하지 않고 현장 정보와 정산 결과를 남긴다.
- 주말특가처럼 생산 시점부터 적용한 행사가격은 해당 영업일의 정상 판매가격으로 집계한다.
- 정가·특가 판매, 할인 단계별 판매와 폐기는 확정 전까지 반드시 `추정`으로 표시한다.

## 금액 용어

- `생산금액`: 당일 생산수량에 정상가를 적용한 금액
- `최종 적용 할인액`: 당일 생산상품에 실제 부착한 최종 할인단계의 할인액
- `할인후생산금액`: 생산금액에서 최종 적용 할인액을 뺀 금액
- `할인판매금액`: 할인상품 가운데 실제 판매가 확인된 금액

같은 상품에 할인 라벨을 여러 단계로 다시 붙였으면 할인액을 중복 합산하지 않고 마지막 할인단계만 적용한다. 당일 생산목록에 없는 상품의 할인은 할인후생산금액에서 제외한다. 할인후생산금액은 라벨 부착 기준 금액이며 실제 매출로 간주하지 않는다.

## 생산량 단위

- 흰다리새우: 원물 1팩은 2kg이다. 12kg 한 박스는 2kg 6팩이다.
- 흰다리새우 30/40 규격은 1kg당 약 30~40마리다. 판매용 한 개에는 약 11마리를 담는다.
- 가리비관자: 10kg 한 박스는 1kg 10팩이다. 원물 1팩에서 7알 구성 판매품 약 4~5개가 나온다.
- 자숙참소라: 10kg 벌크 입고다. 판매용 한 개는 약 170g이다.
- 원물 투입은 `팩` 또는 `kg`, 완성 판매품은 `개`로 기록한다.
- 새우와 관자는 가능하면 원물 투입량과 실제 완성 개수를 모두 센다. 완성 개수가 없을 때만 추정한다.

## 하루 기록 절차

### 1. 생산 완료

품목별로 원물 투입량, 실제 완성 개수, 당일 판매가격을 기록한다. 실제 완성 개수가 없으면 예상 범위와 대표 추정치를 함께 기록한다.

### 2. 퇴근 전 할인

시코드 인쇄기록은 Supabase에서 조회한다. 현장에서는 각 출력 묶음을 다음 중 하나로 구분한다.

- `attached_now`: 워터비 직원이 즉시 부착
- `preprinted_for_mart_staff`: 퇴근 후 마트 직영 직원이 부착하도록 사전 출력
- `misprint_confirmed`: 현장에서 확인된 오출력
- `unknown`: 아직 용도를 확인하지 못한 출력

동일 상품을 짧은 시간 안에 다른 할인율로 다시 출력했더라도 자동으로 오출력 처리하지 않는다. 다음 할인 단계용 사전 출력일 수 있다.

### 3. 다음 날 아침

품목과 할인 단계별로 아래 두 가지를 센다.

- 사용되지 않고 남은 사전 출력 라벨 수
- 상품으로 남아 폐기한 수량

마트 직영 직원에게는 **미사용 라벨을 버리지 말고 품목별로 모아 둘 것**을 요청한다. 아침 확인 담당자가 수량을 센 뒤 폐기한다. 확인 전에 라벨이 버려지면 할인 단계별 판매량을 역산할 수 없으므로 `unknown`으로 기록한다.

계산식은 다음과 같다.

```text
이전 할인 단계 판매 추정 = 다음 단계 사전 출력 라벨 중 아침에 남은 수량
다음 할인 단계 실제 부착 = 다음 단계 사전 출력 수량 - 미사용 라벨 수
다음 할인 단계 판매 추정 = 실제 부착 수량 - 폐기 수량
```

라벨 분실, 추가 출력, 다른 상품 부착 등으로 수량이 맞지 않으면 차이를 숨기지 않고 `variance`로 남긴다.

### 4. 영업일 확정

다음 날 확인이 끝나면 상태를 `finalized`로 바꾸고 아래를 품목별로 확정한다.

- 생산수량
- 정가·주말특가 판매 추정
- 할인 단계별 부착 및 판매 추정
- 폐기
- 미사용 라벨
- 오출력
- 수량 차이

## 저장 위치

- 일일 운영기록: `instances/operations/wolgye/YYYY-MM.yaml`
- 시코드 인쇄 원본: Supabase `print_records_wolgye`
- 할인 통합 조회: Supabase `v_discount_records`

일일 ID는 `ops_wg_YYYYMMDD` 형식을 사용한다. 같은 영업일 기록은 하나만 만들고, 다음 날 마감 정보는 해당 기록의 `nextMorning`에 추가한다.
